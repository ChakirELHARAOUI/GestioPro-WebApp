const config = {
    development: {
      username: 'your_username',
      password: 'your_password',
      database: 'your_database',
      host: 'your_host',
      dialect: 'your_dialect' //postgres if you use postgresql
    }
}

module.exports = config;